# project-db-compat.mdc

## 概念

约定数据库相关规范，防止 AI 写出不兼容 SQL。

## 演示操作

在 Qoder 里输入：

```
帮我在 .qoder/rules/ 下生成 project-db-compat.mdc
type always-on
MySQL 8.0，禁止窗口函数
SQL 关键字大写
禁止在循环里查数据库
```
