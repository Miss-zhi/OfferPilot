# react-component.mdc

## 概念

规范 React 组件写法，统一风格。

## 演示操作

在 Qoder 里输入：

```
帮我在 .qoder/rules/ 下生成 react-component.mdc
type always-on，只对 tsx 生效
函数组件不用 Class，Props 要定义 TS 类型
一个文件一个组件
```
