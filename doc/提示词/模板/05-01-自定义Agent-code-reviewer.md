# code-reviewer

## 概念

Agent 是"指挥官"，组合多个 Skill、调用 Rule，跨越多个层完成一个完整任务。

## 演示操作

在 Qoder 输入：

```
帮我在 .qoder/agents/ 下生成 code-reviewer.md
代码审查 Agent，只读
工具：Read、Grep、Glob
基于 Rule 审查代码，输出审查报告
```
