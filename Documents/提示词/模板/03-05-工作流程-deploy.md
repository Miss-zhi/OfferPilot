# deploy

## 概念

部署上线的标准化步骤。

## 演示操作

在 Qoder 输入 /create-skill，然后输入：

帮我创建一个 Skill，名字是 deploy，功能是执行标准化部署流程。
流程：编译打包、备份数据库、执行变更脚本、部署新版本、验证服务状态。必须包含回滚方案。
文件名规则：MMDD-HHMMSS-部署模块名.md
文档目录：Documents/部署记录
