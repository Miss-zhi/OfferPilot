# react-hooks.mdc

## 概念

规范 Hooks 使用，避免遗漏依赖。

## 演示操作

在 Qoder 里输入：

```
帮我在 .qoder/rules/ 下生成 react-hooks.mdc
type always-on，只对 tsx 生效
useEffect 要写依赖数组
自定义 Hooks 以 use 开头
Hooks 不能写在条件语句里
```
