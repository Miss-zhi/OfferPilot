- Controller 只负责参数校验与响应封装，所有持久化与外部调用必须经 Service 层编排
- 异常采用 BusinessException 基类派生具体错误类型，由 GlobalExceptionHandler 统一映射为 ApiResponse
- 配置项通过 @ConfigurationProperties 绑定 application.yml，敏感值使用 ${ENV_VAR} 占位符注入
- 实体统一继承 BaseEntity 并使用 @EnableJpaAuditing 自动维护创建/修改时间