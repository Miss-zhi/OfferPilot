- API 路径集中定义在 `infra/constants.ts`，`service/*.ts` 仅引用常量而非硬编码字符串
- 所有 HTTP 请求经 `infra/http.ts` 的 Axios 实例发出，由拦截器统一附加 Authorization 头并解包 ApiResponse
- 跨组件状态统一放在 `store/` 下使用 Zustand create 创建，页面组件通过 `useXxxStore` hook 订阅
- 页面按业务域在 `ui/pages/<domain>/` 下自包含，通用 UI 片段抽取到 `ui/components/` 供多页面复用