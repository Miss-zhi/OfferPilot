- DTO 统一使用 Lombok 三件套注解 `@Data @NoArgsConstructor @AllArgsConstructor`，保持无参构造用于 JSON 反序列化。
- 正则表达式以 `private static final Pattern` 常量形式声明在类顶部，避免每次匹配重复编译。
- 空输入快速返回空结果对象而非抛异常，由调用方根据字段判空处理（如 `pdfUrl.isBlank()` 返回全空 `ResumeParseResult`）。