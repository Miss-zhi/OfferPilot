- 所有常量对象（API 路径、存储键、上传限制）均以 `export const X = { ... } as const` 形式集中定义在 `infra/constants.ts`，禁止在业务组件中硬编码字符串。
- 受保护路由统一通过 `App.tsx` 内嵌的 `AuthGuard` 高阶组件包裹，而非在每个页面组件内自行判断认证状态。
- HTTP 调用一律通过 `infra/http.ts` 导出的 axios 实例发起，由拦截器统一处理 Token 注入与 401 跳转，业务代码无需关心鉴权细节。