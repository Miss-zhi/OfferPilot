- 异步操作统一使用 try/catch 包裹，成功时调用 `message.success(...)`，失败时调用 `message.error(...)`，并在 finally 中重置 loading 状态。
- 副作用型加载函数用 `useCallback` 包裹并通过 `useEffect` 在挂载时触发，避免重复请求。
- 表单校验通过 Antd Form.Item 的 `rules` 声明式定义，提交前调用 `form.validateFields()` 再构造 DTO 传给 service。