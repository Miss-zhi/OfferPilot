开发环境通过 `docker-compose up -d` 启动 MySQL/Redis/Milvus(etcd+minio)/MCP WebSearch 基础设施；应用默认走 dev profile 使用 H2 文件库，无需外部 DB；运行需设置环境变量 `DASHSCOPE_API_KEY`（LLM）、`EMBEDDING_API_KEY`（Embedding，可复用）、`TRANSCRIPTION_API_KEY`（语音转写）、`JWT_SECRET`、`ENCRYPTION_SECRET_KEY`。