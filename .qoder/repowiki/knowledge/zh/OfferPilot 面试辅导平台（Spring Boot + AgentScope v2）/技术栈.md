Spring Boot 3.2.5 + Spring Security + Spring Data JPA + H2(开发)/MySQL(生产)；AgentScope Java v2 (agentscope-core/harness + openai/dashscope/anthropic/gemini/ollama Provider SPI)；Milvus 2.4.1 SDK（IVF_FLAT+COSINE 向量索引）；Redis + Caffeine 双层缓存；JWT (jjwt 0.12.5)；OkHttp 调用 DashScope Embedding/Transcription；PDFBox + POI 文档解析；Testcontainers + REST Assured 集成测试。