启动需先运行 `docker-compose up -d` 拉起 MySQL、Milvus、Redis 三个服务；本地开发默认激活 `dev` profile（H2 + DEBUG 日志），生产切换 `prod` profile（MySQL + INFO 日志）。所有密钥（DASHSCOPE_API_KEY、EMBEDDING_API_KEY、TRANSCRIPTION_API_KEY、JWT_SECRET、ENCRYPTION_SECRET_KEY、MYSQL_*）必须通过环境变量或 `.env` 文件注入，不可硬编码到配置文件。