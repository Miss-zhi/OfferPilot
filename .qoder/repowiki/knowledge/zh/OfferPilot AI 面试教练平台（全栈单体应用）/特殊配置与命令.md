开发环境通过 docker-compose.yml 一键启动 MySQL/Redis/Milvus(etcd+MinIO)/open-websearch 等基础设施；应用本身通过 Maven 或 IDE 本地运行，无需打包进容器。环境变量需设置 DASHSCOPE_API_KEY、MYSQL_ROOT_PASSWORD、MINIO_ACCESS_KEY 等；MCP web-search 服务默认监听 3000 端口，AgentFactory 中硬编码连接 http://localhost:3003/mcp（注意 compose 映射为 3002:3000，实际应访问 3002）。